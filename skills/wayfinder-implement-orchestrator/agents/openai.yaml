interface:
  display_name: "Wayfinder Implementation Dispatcher"
  short_description: "Dispatch tickets to isolated Codex tasks"
  default_prompt: "Use $wayfinder-implement-orchestrator to dispatch these existing implementation tickets."
policy:
  allow_implicit_invocation: false
