interface:
  display_name: "Ticket Sizing"
  short_description: "预测逐票 token 需求，审查独立验收与依赖边界"
  default_prompt: "使用 $ticket-sizing 预测每票 token 需求并审查拆票粒度，展示逐票评估结果。"
policy:
  allow_implicit_invocation: true
